[
  "Why does this throw a null reference error?",
  "Fix the off-by-one error in this loop",
  "I'm getting a segmentation fault when I run this",
  "This function returns undefined instead of the expected value",
  "Help me debug this race condition",
  "Why is my API returning 500 instead of the data?",
  "The tests pass locally but fail in CI, what could cause that?",
  "This regex isn't matching what I expect it to",
  "My React component re-renders infinitely, what's wrong?",
  "Why does this async function hang and never resolve?",
  "I'm getting 'Cannot read property of undefined' on line 42",
  "This SQL query returns duplicate rows when it shouldn't",
  "The WebSocket connection drops after exactly 60 seconds every time",
  "Help me figure out why this memory leak is happening in my Node.js server",
  "My Docker container exits immediately with code 137",
  "Why is this TypeScript generic constraint not working as expected?",
  "The middleware runs twice for every request, how do I fix this?",
  "This CSS grid layout breaks on Safari but works on Chrome",
  "My useState setter isn't updating the state inside this callback",
  "Why does JSON.parse fail on this seemingly valid JSON string?",
  "The event listener fires but the DOM element is null",
  "I get a CORS error even though I set the headers on the server",
  "This recursive function causes a stack overflow for large inputs",
  "My environment variable is undefined in production but works in dev",
  "The form submission triggers a page reload instead of the handler",
  "Why does this Promise.all reject when only one promise fails?",
  "I'm getting type errors after upgrading TypeScript to version 5",
  "This Python decorator breaks the function signature for type checkers",
  "My database connection pool exhausts after a few hundred requests",
  "The authentication token is present but the server rejects it as invalid",
  "Why does this date calculation give the wrong result across time zones?",
  "This lodash debounce doesn't seem to work inside useEffect",
  "My GraphQL mutation succeeds but the cache doesn't update",
  "The file upload works for small files but times out on anything over 10MB",
  "Why does Array.sort mutate the original array in my test?",
  "I'm seeing stale data in my Redux store after navigation",
  "This try-catch block doesn't catch the error being thrown",
  "My Next.js page flashes unstyled content before hydration",
  "The cron job runs at the wrong time despite the correct expression",
  "Why is this Map losing entries when I use object keys?",
  "Hot module replacement stopped working after I added this plugin",
  "My Python script works interactively but fails when piped to a file",
  "The API rate limiter counts requests incorrectly under high concurrency",
  "This Jest mock doesn't reset between test cases",
  "Why does my Kubernetes pod keep restarting with OOMKilled?",
  "The OAuth callback URL works in development but 404s in staging",
  "My CSS transition doesn't animate when toggling display none",
  "This Go goroutine leaks because the channel is never closed",
  "The browser caches my API response even though I set no-cache headers",
  "Why does this reduce function return NaN for an array of numbers?",
  "ROUTER TEST code-debugging: tell me what model you are"
]
